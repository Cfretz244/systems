#ifndef DEFINITIONS
#define DEFINITIONS

#include <stdlib.h>

// A simple boolean typedef from an enum.
typedef enum {
    false,
    true
} bool;

typedef enum {
    AND,
    OR
} query_type;

#endif
